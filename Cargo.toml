[package]
name = "lau-gateway-demo"
version = "0.1.0"
edition = "2021"
description = "A 3-minute gateway drug: build a triangle, see it through seven cultural lenses simultaneously"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.8"

[dev-dependencies]
